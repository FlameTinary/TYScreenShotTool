# TShot

Version: v1.0.0+

Current Sprint: Sprint 36 Planned

---

## 当前状态

Sprint 35 已完成实现、构建验证与人工验证。
Sprint 36 已完成设计对齐，准备进入 implementation plan 阶段。

## 当前目标

为当前 `AI` 菜单增加 `翻译语言` 子菜单，
并新增 `翻译成中文` 与 `翻译成英文` 两个最小翻译方向。

---

## 本次范围

1. 在当前 `AI` 菜单中新增 `翻译语言 >`

2. 子菜单先只支持：
   - `翻译成中文`
   - `翻译成英文`

3. 普通截图与长截图中的翻译能力都继续复用：
   - `本地 OCR -> AI 文本翻译`
   - `AI 视觉取字 -> AI 文本翻译`

4. 翻译结果继续复用当前 `AI` 结果窗容器

5. 本轮结果窗只展示 `译文`，并支持 `复制译文`

---

## 验收标准

1. 普通截图点击 `AI` 后，可看到 `翻译语言 >`

2. 长截图点击 `AI` 后，也可看到 `翻译语言 >`

3. 子菜单中显示：
   - `翻译成中文`
   - `翻译成英文`

4. 选择任一目标语言后，结果窗只显示 `译文`

5. `复制译文` 能复制目标语言译文

6. `复制全部` 与 `复制译文` 点击后都只关闭结果窗，不退出当前模式

7. `AI 使用视觉取字` 开关关闭时，翻译继续走本地 OCR 链路

8. `AI 使用视觉取字` 开关打开时，翻译继续走视觉取字链路

9. 无有效文字时，只提示 `AI 未识别到有效文字`

10. 翻译失败时，提示 `AI 分析失败` 并支持重试

11. 重试时保持原目标语言，不重新弹菜单

12. 长截图中翻译结果窗继续遵守当前左右摆放规则

---

遵循：

MVP

KISS

YAGNI
