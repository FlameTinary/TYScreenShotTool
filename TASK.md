# TShot

Version: v1.0.0+

Current Sprint: Sprint 36 Done

---

## 当前状态

Sprint 36 已完成实现、构建验证与人工验证。
AI 菜单中的翻译语言能力已交付，并完成本轮问题收口。

## 当前目标

完成 Sprint 36 收尾同步，
并为后续 Sprint 预留新的当前任务切换入口。

---

## 本次范围

1. 在普通截图与长截图的 `AI` 菜单中新增 `翻译语言 >`

2. 本轮只支持：
   - `翻译成中文`
   - `翻译成英文`

3. 翻译继续复用两条输入链路：
   - `本地 OCR -> AI 文本翻译`
   - `AI 视觉取字 -> AI 文本翻译`

4. 翻译结果继续复用当前 `AI` 结果窗容器

5. 结果窗只展示 `译文`，并支持：
   - `复制全部`
   - `复制译文`

---

## 验收标准

1. 普通截图与长截图点击 `AI` 后，都可看到 `翻译语言 >`

2. 子菜单中显示：
   - `翻译成中文`
   - `翻译成英文`

3. 翻译结果窗只展示 `译文`

4. `复制全部` 与 `复制译文` 都只关闭结果窗，不退出当前模式

5. `AI 使用视觉取字` 开关关闭与打开两种状态下，翻译都可正常工作

6. 无有效文字时，会看到 `AI 未识别到有效文字`

7. 翻译失败时，提示 `AI 分析失败` 并支持重试

8. 重试时保持原目标语言，不重新弹菜单

9. 长截图结果窗继续遵守当前左右摆放规则与过期保护

---

遵循：

MVP

KISS

YAGNI
