# TShot

Version: v1.0.0+

Current Sprint: Sprint 37 Planned

---

## 当前状态

Sprint 36 已完成实现、构建验证与人工验证。
Sprint 37 已完成设计对齐，准备进入 implementation plan 阶段。

## 当前目标

为当前 `AI` 菜单新增一级项 `界面结构识别`，
并输出可供设计与开发继续复用的结构化描述。

---

## 本次范围

1. 在普通截图与长截图的 `AI` 菜单中新增一级项 `界面结构识别`

2. 网页与 App 截图都可使用，
   但本轮先按通用结构输出

3. 继续复用两条输入链路：
   - `本地 OCR -> AI 界面结构识别`
   - `AI 视觉取字 -> AI 界面结构识别`

4. 结果继续复用当前 `AI` 结果窗容器

5. 结果窗只展示单段结构化文本，
   并支持：
   - `复制全部`
   - `复制结构`

---

## 验收标准

1. 普通截图与长截图点击 `AI` 后，都可看到一级菜单项 `界面结构识别`

2. 结果窗继续复用当前 `AI` 结果窗容器

3. 结果窗只展示一段结构化文本

4. 结构化文本固定包含：
   - `组件识别`
   - `结构层级`
   - `视觉特征`
   - `交互语义`
   - `实现提示`

5. `复制全部` 与 `复制结构` 都只关闭结果窗，不退出当前模式

6. `AI 使用视觉取字` 开关关闭与打开两种状态下，能力都可正常工作

7. 无有效内容时，只提示 `AI 未识别到有效内容`

8. 失败时提示 `AI 分析失败` 并支持重试

9. 重试时保持原分析方向，不重新弹菜单

10. 长截图结果窗继续遵守当前左右摆放规则与过期保护

---

遵循：

MVP

KISS

YAGNI
