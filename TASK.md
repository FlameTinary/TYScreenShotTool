# TShot

Version: v1.0.0+

Current Sprint: Sprint 35 Planned

---

## 当前状态

Sprint 34 已完成实现、构建验证与人工验证。
Sprint 35 已完成设计对齐，准备进入 implementation plan 阶段。

## 当前目标

为 `AI` 按钮增加最小菜单入口，
并新增 `摘要总结` 这一项通用 AI 能力。

---

## 本次范围

1. 点击 `AI` 按钮后先弹出最小菜单，而不是直接开始分析

2. Sprint 35 的菜单先只包含两项：
   - `开发报错分析`
   - `摘要总结`

3. `开发报错分析` 保持当前已有行为不变

4. `摘要总结` 作为本轮唯一新增 AI 能力，输出固定 3 条重点

5. 普通截图与长截图中的 `AI` 都使用同一套菜单交互

6. 继续复用 Sprint 34 的本地 OCR / 视觉取字双输入链路

7. 继续复用当前 `AI` 结果窗容器，不新增第二种 AI 结果窗

---

## 验收标准

1. 普通截图点击 `AI` 后，先弹出最小菜单

2. 长截图点击 `AI` 后，也先弹出同样菜单

3. 菜单中显示：
   - `开发报错分析`
   - `摘要总结`

4. 选择 `开发报错分析` 后，行为与当前版本保持一致

5. 选择 `摘要总结` 后，能够输出固定 3 条重点摘要

6. `AI 使用视觉取字` 开关关闭时，两种方向都继续走本地 OCR 链路

7. `AI 使用视觉取字` 开关打开时，两种方向都继续走视觉取字链路

8. 无有效文字时，只提示 `AI 未识别到有效文字`，不保留结果窗

9. 分析失败时，提示 `AI 分析失败`，并支持重试

10. 重试时保持原分析方向，不重新弹菜单

---

遵循：

MVP

KISS

YAGNI
