# TShot

Version: v1.0.0+

Current Sprint: Sprint 34 Planned

---

## 当前状态

Sprint 33 已完成实现、构建验证与人工验证。
Sprint 34 已完成设计文档，准备进入实现计划阶段。

## 当前目标

在 `Settings` 页面中增加 `AI 使用视觉取字` 开关，
让 `AI` 分析可以在“本地 OCR 链路”和“AI 视觉取字链路”之间切换，
并同时覆盖普通截图与长截图模式。

---

## 本次范围

1. 在 `Settings` 页面中新增 `AI 使用视觉取字` 开关，默认关闭

2. 开关关闭时，普通截图与长截图中的 `AI` 继续走：
   `截图 -> 本地 OCR -> AI 文本分析 -> AI 结果窗`

3. 开关打开时，普通截图与长截图中的 `AI` 改走：
   `截图 -> AI 视觉取字 -> AI 文本分析 -> AI 结果窗`

4. 新增 `AIImageTextExtractionService`，负责基于截图图片调用视觉模型提取文字

5. 保持 `OCR` 按钮与 `OCR` 结果预览窗口原样，不受此开关影响

6. 保持当前 `AI` 结果窗结构、重试逻辑与长截图异步失效保护

7. 将“AI 多方向菜单”仅记录为后续需求，不进入本轮实现

---

## 验收标准

1. `Settings` 中新增 `AI 使用视觉取字` 开关，默认关闭

2. 开关关闭时，普通截图点击 `AI` 后，行为与当前版本保持一致

3. 开关关闭时，长截图点击 `AI` 后，行为与当前版本保持一致

4. 开关打开时，普通截图点击 `AI` 后，能够基于视觉模型提取文字再继续分析

5. 开关打开时，长截图点击 `AI` 后，能够基于视觉模型提取文字再继续分析

6. 开关关闭时，本地 OCR 无有效文字，仍提示 `OCR 未识别到有效文本`

7. 开关打开时，视觉取字失败，提示 `AI 识别失败`，并支持重试

8. 开关打开时，视觉取字成功但没有有效文字，只提示 `AI 未识别到有效文字`，不打开结果窗

9. 两条链路中任一条文本分析失败时，提示 `AI 分析失败`，并支持重试

10. `OCR`、复制、保存、`Pin`、热键设置、保存目录设置主链路不回归

---

遵循：

MVP

KISS

YAGNI
