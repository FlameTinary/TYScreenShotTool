# SmartShot

Version: V0.6

Current Sprint: Sprint 24 Planned

---

## 当前目标

在已完成 `OCR` 与 `Pin` 最小动作链路的基础上，
补齐截图反馈体验，
并增强 `Pin` 浮动窗口的最小可用展示与缩放能力。


---

## 本次范围

1. 增加 `Toast` 提示能力

2. 在 `OCR`、复制、保存完成后弹出对应提示

3. 优化 `Pin` 浮动窗口默认尺寸

4. 优化 `Pin` 浮动窗口默认位置到屏幕右上角

5. 支持通过拖拽窗口边和角调整 `Pin` 窗口大小

6. 保持现有截图、标注、复制、保存、`OCR`、`Pin` 主链路不被破坏

7. 不扩展到长截图与 AI 分析

8. 不改变当前标注、马赛克、圆角、阴影与截图区域微调主链路

9. 遵循 MVP / KISS / YAGNI

---

## 验收标准

`OCR`、复制、保存完成后应弹出对应 `Toast` 提示。

点击 `Pin` 后，浮动窗口应以较小默认尺寸显示在屏幕右上角。

用户应可通过拖拽窗口边和角调整 `Pin` 窗口大小。

截图、标注、复制、保存、`OCR`、`Pin` 主链路应保持正常。

---

遵循：

MVP

KISS

YAGNI
