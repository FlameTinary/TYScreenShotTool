# TShot

Version: V0.8

Current Sprint: Sprint 29 Completed

---

## 当前目标

完成面向开发报错截图的最小 `AI 分析` 能力。

主链路：

`截图 -> OCR -> AI 分析 -> 结果展示 -> 复制`

---

## 本次范围

1. 编辑工具栏中可见并可触发 `AI` 按钮

2. 点击 `AI` 后对原始截图区域执行 `OCR`

3. 将 `OCR` 文本发送给单一 AI 提供方执行最小分析

4. 在当前截图流程中展示轻量结果面板

5. 结果面板支持 `复制`、`重试`、`关闭`

6. 使用本地隐藏配置提供 `API Key`

7. 缺少配置、OCR 空结果、请求失败、AI 空结果时给出明确提示

---

## 验收标准

1. 编辑态工具栏中可见并可点击 `AI` 按钮

2. 点击 `AI` 后会先执行 `OCR`，再进入最小 loading 状态

3. 对开发报错截图可返回一段可读分析，至少包含：
   - 报错大意
   - 可能原因
   - 建议下一步

4. 分析结果支持复制、重试、关闭

5. API Key 缺失、OCR 为空、请求失败、空结果时都有明确提示

6. 不影响现有截图、标注、保存、复制、OCR、Pin、长截图主链路

---

## 当前结论

Sprint 29 已完成实现与真实验证。

当前已确认：

1. `AI` 分析支持本地隐藏 `API Key`、`Base URL` 与模型配置

2. 官方 OpenAI 与第三方兼容网关都可按 `Responses API` 方式接入

3. App Sandbox 下已补齐网络客户端权限，真实联网请求可正常发出

---

遵循：

MVP

KISS

YAGNI
