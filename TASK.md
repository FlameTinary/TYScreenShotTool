# TShot

Version: v1.0.0+

Current Sprint: Sprint 33 Done

---

## 当前状态

Sprint 33 已完成实现、构建验证与人工验证。

## 当前目标

在 `Settings` 页面中完成最小自定义截图热键能力，
让用户可以在 `Settings` 中直接录入热键组合并立即生效。

---

## 本次范围

1. 将 `Settings` 中的截图快捷键配置从预设菜单调整为可点击编辑框

2. 点击编辑框后进入热键编辑模式

3. 支持录入 `Command / Shift / Option / Control` 与 `字母 / 数字 / 功能键 / 方向键` 组合

4. 用户按 `Return` 后确认热键并立即尝试生效

5. 用户按 `Esc` 或点击编辑框外部后取消本次编辑并恢复旧热键

6. 热键注册失败时恢复旧热键并给出明确提示

7. 保存目录配置与现有截图主链路保持正常

---

## 验收结果

1. `Settings` 中的截图快捷键配置已从预设菜单调整为可点击编辑框

2. 用户点击编辑框后，可进入热键编辑模式，并在录制态看到高亮反馈

3. 支持录入 `Command / Shift / Option / Control` 与 `字母 / 数字 / 功能键 / 方向键` 组合，并实时显示结果

4. 用户只按修饰键时不会保存为有效热键，按 `Return` 后会给出“请至少输入一个主键”提示

5. 用户按 `Return` 后，若组合有效且注册成功，新热键会立即生效

6. 用户按 `Return` 后，若注册失败，界面会恢复旧热键并提示“快捷键注册失败，请更换组合”

7. 用户按 `Esc` 或点击编辑框外部后，会取消本次编辑、退出高亮并恢复旧热键

8. 重新打开 `Settings` 后，热键编辑高亮与错误提示不会残留

9. 保存目录配置与其他现有设置页行为保持正常

10. 已执行 `./scripts/build.sh`，构建通过；人工验证通过

---

遵循：

MVP

KISS

YAGNI
