# TShot

Version: v1.0.0+

Current Sprint: Sprint 33 Planned

---

## 当前状态

Sprint 32 已完成实现、构建验证与人工验证。
Sprint 33 已完成设计文档，准备进入实现计划阶段。

## 当前目标

在 `Settings` 页面中补齐最小自定义截图热键能力，
让用户可以点击截图快捷键编辑框，
直接录入自己的热键组合并按回车确认生效。

---

## 本次范围

1. 将 `Settings` 中的截图快捷键配置从预设菜单调整为可点击编辑框

2. 点击编辑框后进入热键编辑模式

3. 支持录入 `Command / Shift / Option / Control` 与 `字母 / 数字 / 功能键 / 方向键` 组合

4. 用户按 `Return` 后确认热键并立即尝试生效

5. 用户按 `Esc` 或点击编辑框外部后取消本次编辑并恢复旧热键

6. 热键注册失败时恢复旧热键并给出明确提示

7. 保存目录配置与现有截图主链路保持正常

---

## 验收标准

1. 用户进入 `Settings` 后，可看到“截图快捷键”对应的可点击编辑框，而不是预设菜单

2. 点击编辑框后，进入热键编辑模式

3. 用户输入 `Command + A` 时，编辑框可实时显示该组合

4. 用户输入 `Command + Shift + 8`、`Control + Option + F5`、`Command + ↑` 等组合时，编辑框可正确显示

5. 用户只按修饰键时，不能直接确认为有效热键

6. 用户按 `Return` 后，若组合有效且注册成功，新热键立即生效

7. 用户按 `Return` 后，若注册失败，界面恢复旧热键并给出失败提示

8. 用户按 `Esc` 或点击编辑框外部后，会取消编辑并恢复旧热键

9. 保存目录配置与其他现有设置页行为保持正常

---

遵循：

MVP

KISS

YAGNI
