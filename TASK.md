# TShot

Version: v1.0.0+

Current Sprint: Sprint 31 Completed

---

## 当前状态

Sprint 31 已完成并通过人工验证。

## 当前目标

已完成最小 `窗口悬停选择` 起手体验，
让用户在截取整个应用窗口时，
可以通过：

`悬停窗口 -> 点击确认`

直接进入现有截图编辑态。

---

## 本次范围

1. 热键触发后，Overlay 保持统一选择模式

2. 鼠标移动到可识别应用窗口上方时，显示与窗口边界一致的候选高亮框

3. 用户轻点鼠标左键时，确认当前候选窗口并进入现有编辑态

4. 用户按下鼠标后开始拖拽时，立即切回现有自由框选流程

5. 鼠标位于桌面空白区域或无法识别有效窗口时，回退为当前普通待命状态

6. 现有复制、保存、`OCR`、`AI`、`Pin`、长截图编辑态主链路保持不变
7. 窗口存在重叠时，复制、保存、`OCR`、`AI`、`Pin` 导出结果仍应保持目标窗口完整内容

---

## 验收标准

1. 热键触发截图后，鼠标移动到普通应用窗口上方时，可看到与窗口边界一致的候选高亮框

2. 鼠标继续移动到其他窗口时，候选高亮会跟随切换

3. 鼠标位于桌面空白区域时，不显示候选窗口高亮，Overlay 保持普通待命状态

4. 当存在候选窗口时，用户轻点鼠标左键，应用直接以该窗口矩形进入现有编辑态

5. 当用户按下鼠标后开始拖拽时，应用会放弃候选窗口，并按现有自由框选流程运行

6. 通过窗口悬停确认进入编辑态后，复制、保存、`OCR`、`AI`、`Pin`、长截图主链路保持正常

---

遵循：

MVP

KISS

YAGNI
