# TShot

Version: v1.0.0+

Current Sprint: Sprint 37 Done

---

## 当前状态

Sprint 37 已完成实现、构建验证与人工验证。
当前分支已完成 `界面结构识别` 能力收口，等待进入下一轮 Sprint 规划。

## 当前目标

完成 Sprint 37 `界面结构识别` 收尾，
并保持后续 Sprint 继续在当前 `AI` 菜单结构上扩展。

---

## 本次范围

1. 普通截图与长截图的 `AI` 菜单都已支持一级项 `界面结构识别`

2. 网页与 App 截图都可使用，
   本轮统一按通用结构输出

3. 两条输入链路均已接入：
   - `本地 OCR -> AI 界面结构识别`
   - `AI 视觉取字 -> AI 界面结构识别`

4. 结果继续复用当前 `AI` 结果窗容器

5. 结果窗展示单段结构化文本，
   并支持：
   - `复制全部`
   - `复制结构`

---

## 验收标准

1. 普通截图与长截图点击 `AI` 后，都可看到一级菜单项 `界面结构识别`

2. 结果窗继续复用当前 `AI` 结果窗容器

3. 结果窗只展示一段结构化文本

4. 结构化文本固定包含：
   - `组件识别`
   - `结构层级`
   - `视觉特征`
   - `交互语义`
   - `实现提示`

5. `复制全部` 与 `复制结构` 都只关闭结果窗，不退出当前模式

6. `AI 使用视觉取字` 开关关闭与打开两种状态下，能力都可正常工作

7. 无有效内容时，只提示 `AI 未识别到有效内容`

8. 失败时提示 `AI 分析失败` 并支持重试

9. 重试时保持原分析方向，不重新弹菜单

10. 长截图结果窗继续遵守当前左右摆放规则与过期保护

---

遵循：

MVP

KISS

YAGNI
