# TShot

Version: v1.0.0+

Current Sprint: Sprint 34 Done

---

## 当前状态

Sprint 34 已完成实现、构建验证与人工验证。
相关文档已完成同步收尾。

## 当前目标

完成 `AI 视觉取字分析` 这一轮迭代收尾，
确认双链路切换能力已经稳定落地。

---

## 本次结果

1. `Settings` 页面已新增 `AI 使用视觉取字` 开关，默认关闭

2. 开关关闭时，普通截图与长截图中的 `AI` 保持原有
   `截图 -> 本地 OCR -> AI 文本分析 -> AI 结果窗` 链路

3. 开关打开时，普通截图与长截图中的 `AI` 已切换为
   `截图 -> AI 视觉取字 -> AI 文本分析 -> AI 结果窗` 链路

4. 已新增 `AIImageTextExtractionService`，负责基于截图图片调用视觉模型提取文字

5. `OCR` 按钮、`OCR` 结果预览窗口与当前 `AI` 结果窗结构保持不变

6. 长截图 `AI` 分析继续保留原有异步失效保护

7. 已补充控制台日志，可明确看到当前 `AI` 分析走的是本地 OCR 还是视觉取字链路

---

## 验收结果

1. `Settings` 中新增 `AI 使用视觉取字` 开关，默认关闭，验证通过

2. 开关关闭时，普通截图与长截图点击 `AI` 后，行为与旧版本一致，验证通过

3. 开关打开时，普通截图与长截图点击 `AI` 后，能够基于视觉模型提取文字再继续分析，验证通过

4. 本地 OCR 无有效文字时，仍提示 `OCR 未识别到有效文本`，验证通过

5. 视觉取字失败时，提示 `AI 识别失败` 并支持重试，验证通过

6. 视觉取字无有效文字时，只提示 `AI 未识别到有效文字`，不保留错误结果窗，验证通过

7. 两条链路中任一条文本分析失败时，提示 `AI 分析失败`，验证通过

8. `OCR`、复制、保存、`Pin`、热键设置、保存目录设置主链路未回归，验证通过

---

遵循：

MVP

KISS

YAGNI
