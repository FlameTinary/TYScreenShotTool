# TShot

Version: v1.0.0+

Current Sprint: Sprint 35 Done

---

## 当前状态

Sprint 35 已完成实现、构建验证与人工验证。
当前 `AI` 能力已经支持菜单分流与摘要总结。

## 当前目标

等待进入下一个 Sprint。

---

## 本次范围

1. 普通截图与长截图中的 `AI` 按钮都先弹出最小菜单

2. 当前菜单包含两项：
   - `开发报错分析`
   - `摘要总结`

3. `摘要总结` 输出固定 3 条重点

4. 两种分析方向都继续支持：
   - `本地 OCR -> AI 文本分析`
   - `AI 视觉取字 -> AI 文本分析`

5. 普通截图与长截图继续复用同一个 `AI` 结果窗容器

---

## 验收结果

1. 普通截图与长截图点击 `AI` 后，都会先弹出最小菜单

2. 菜单中已支持：
   - `开发报错分析`
   - `摘要总结`

3. `AI 使用视觉取字` 开关关闭与打开两种状态下，两类分析方向都已人工验证通过

4. 无有效文字时，仅提示 `AI 未识别到有效文字`，不会保留结果窗

5. 分析失败时会展示错误提示，并支持重试且保持原分析方向

6. 普通截图与长截图中点击 `复制全部` / `复制重点` 后，都只关闭 `AI` 结果窗，不退出当前模式

7. 长截图预览窗与 `AI` / `OCR` 结果窗的左右摆放关系已人工验证通过

---

遵循：

MVP

KISS

YAGNI
