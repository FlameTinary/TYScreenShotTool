# TShot

Version: v1.0.0+

Current Sprint: Sprint 32 Done

---

## 当前状态

Sprint 31 已完成并通过人工验证。
Sprint 32 已完成实现、构建验证与人工验证。

## 当前目标

完成长截图模式下最小 `OCR` 与 `AI 分析` 能力闭环，
让用户可以在长截图过程中，
直接基于当前已生成的长图结果执行识别与分析，
并在关闭结果窗后继续滚动追加。

---

## 本次范围

1. 长截图底部操作面板增加 `OCR` 与 `AI` 两个入口

2. `OCR` 与 `AI` 基于当前已经拼接出的长图结果执行

3. 进入 `OCR` 或 `AI` 后不结束长截图会话

4. 关闭结果窗后允许用户继续滚动追加长截图

5. 长截图模式下同一时刻只保留一个结果窗

6. `OCR` 与 `AI` 结果窗位置根据长截图预览窗所在侧反向摆放

7. 现有长截图 `复制`、`保存`、`取消`、继续滚动追加主链路保持正常

8. `AI` 分析窗口在屏幕空间不足时，底部按钮支持自动换行显示

---

## 验收结果

1. 进入长截图模式后，底部操作面板可见 `取消 / OCR / AI / 保存 / 复制`

2. 即使用户还未继续滚动，也可以基于首帧长图执行 `OCR` 与 `AI`

3. 点击 `OCR` 后，会基于当前长图结果展示 `OCR` 预览窗口，关闭后仍可继续滚动追加长截图

4. 点击 `AI` 后，会基于当前长图结果展示 `AI` loading / 结果 / 失败状态，关闭后仍可继续滚动追加长截图

5. 长截图预览窗位于左侧时，`OCR` / `AI` 结果窗优先出现在右侧；反之亦然

6. `OCR` 与 `AI` 互相切换时只保留一个结果窗，旧的 `AI` 异步结果不会晚到重新弹出

7. 长截图模式下的 `保存 / 复制 / 取消 / 继续滚动追加` 主链路保持正常

8. `AI` loading 中切换到 `OCR` 或继续滚动后，旧的异步结果不会晚到重新弹出

9. `AI` 分析窗口在窄空间下按钮会自动换行，不再被裁切

---

已完成：

- `./scripts/build.sh` 构建通过
- 长截图 `OCR` / `AI` 主链路人工验证通过
- `AI` 结果窗窄空间按钮换行修复人工验证通过

---

遵循：

MVP

KISS

YAGNI
