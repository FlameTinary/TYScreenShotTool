# Roadmap

## 状态约定

可用状态仅包括：

- `📝 Planned`：已进入 Roadmap，尚未开始
- `🚧 In Progress`：当前正在执行
- `✅ Done`：已完成并验证

维护规则：

- Sprint 按顺序排列
- 默认将第一个状态不是 `✅ Done` 的 Sprint 视为当前 Sprint

## 已完成

### Sprint 01

项目初始化

状态：
✅ Done

---

### Sprint 02

全局热键

状态：
✅ Done

---

### Sprint 03

Overlay 选区

状态：
✅ Done

---

### Sprint 04

Capture Session State Machine

状态：
✅ Done

---

### Sprint 05

ScreenCaptureKit 集成

状态：
✅ Done

成果：

- 获取 CGImage
- 多显示器支持
- Retina 支持
- 权限检查

---

### Sprint 06

PNG 自动保存

状态：
✅ Done

成果：

- `CGImage` 保存为 PNG
- 自动生成毫秒级文件名
- 保存到真实桌面
- Console 输出保存路径
- 修复坐标转换偏移问题

---

### Sprint 07

Clipboard 支持

状态：
✅ Done

成果：

- 截图后自动复制到剪贴板
- 成功后桌面最终生成 PNG
- 控制台输出保存路径与复制成功日志
- 引入临时 PNG 流程以满足失败回滚语义

---

### Sprint 08

OCR

状态：
✅ Done

成果：

- 截图成功后进入 OCR 链路
- 控制台输出识别成功日志
- 控制台输出识别文本内容
- 保持 PNG 保存与剪贴板复制链路正常

---

### Sprint 09

设置页面

状态：
✅ Done

成果：

- 增加菜单栏 `Settings` 入口
- 增加最小设置页面骨架
- 展示 `HotKey 配置`、`保存目录配置`、`OCR 开关` 占位项
- 修复 `Settings` 首次打开时机问题
- 保持截图、PNG、Clipboard、OCR 主链路正常

---

### Sprint 10

OCR 开关

状态：
✅ Done

成果：

- 在设置页面中提供可操作的 OCR 开关
- 开关状态能够真实影响截图后的 OCR 执行流程
- 保持 PNG 保存与剪贴板复制链路不受影响
- OCR 关闭时输出明确跳过日志

---

### Sprint 11

HotKey 配置

状态：
✅ Done

成果：

- 在设置页面中提供可操作的 HotKey 配置
- 允许用户修改截图快捷键
- 修改后的快捷键能够真实触发截图流程
- 快捷键修改后立即生效
- 保持 PNG 保存、剪贴板复制、OCR 主链路正常

---

### Sprint 12

保存目录配置

状态：
✅ Done

成果：

- 在设置页面中提供可操作的保存目录配置
- 允许用户修改截图保存目录
- 修改后的保存目录能够真实影响 PNG 输出位置
- 支持恢复默认桌面
- 保持剪贴板复制与 OCR 链路正常

---

### Sprint 13

截图工具栏基础

状态：
✅ Done

成果：

- 框选完成后不再立刻自动复制、保存或 OCR
- 进入截图编辑态并展示底部工具栏
- 支持用户主动选择复制、保存或取消本次截图
- 增加顶部悬浮设置栏：尺寸、圆角、阴影
- 圆角、阴影样式可进入最终导出 PNG
- 保持实现范围为最小工具栏基础链路

---

### Sprint 14

上架适配

状态：
✅ Done

成果：

- 开启并适配 App Sandbox
- 梳理 Screen Recording 权限与上架说明
- 调整保存目录能力以兼容沙箱权限模型
- 补齐最小上架所需的工程与隐私配置
- 完成 Release 构建、Archive、上传与上架验证链路
- `v1.0.0` 已通过审核并正式上架 App Store
- App Store 地址：
  `https://apps.apple.com/cn/app/tshot/id6780356949?mt=12`

---

### Sprint 15

基础标注工具

状态：
✅ Done

成果：

- 增加矩形、圆形、箭头标注
- 增加自由画笔
- 增加文字标注
- 标注内容可进入最终复制与保存输出
- 修复预览标注与最终导出结果位置不一致问题

---

## 当前状态

暂无进行中的 Sprint。

当前有效的 UI 开发口径：

- 当前主要业务 UI 现状以 AppKit 实现，布局常用 SnapKit
- 后续开发默认推荐 AppKit + SnapKit
- 但这不是唯一限制；若评估后 SwiftUI 或手动 `frame` 更适合具体场景，也可按场景选用

---

### Sprint 46

测试补齐

状态：
✅ Done

目标：

- 建立正式 `XCTest` 单元测试基座
- 补齐第一批稳定逻辑单元测试
- 新增自动测试文档与当前 Sprint 人工测试附录

成果：

- 新增 `TYScreenShotToolTests` 测试 target
- 新增共享 scheme，支持 `xcodebuild test`
- 首批 `96` 个自动测试用例全部通过
- 新增自动测试说明文档
- 新增 Sprint 46 人工测试附录

---

### Sprint 45

纯 AppKit + SnapKit UI 迁移

状态：
✅ Done

目标：

- 移除业务 UI 对 SwiftUI 的依赖
- 将入口、菜单栏、Settings、结果窗和长截图 UI 统一迁移为纯 AppKit + SnapKit
- 清理 `NSHostingView`、`NSHostingController`、`NSViewRepresentable`

成果：

- App 入口改为 AppDelegate + NSStatusItem
- Settings 改为纯 AppKit 页面，5 个完整 section
- OCR / AI 结果窗内容层改为纯 AppKit
- 长截图控制面板与预览窗改为纯 AppKit
- 语言切换后菜单栏、Settings 窗口实时刷新
- 窄宽度场景下结果窗与设置项布局保持可用
- 启动入口已收口为纯 `@main AppDelegate`，菜单栏图标与全局截图热键恢复正常
- 文档同步更新为 AppKit + SnapKit 技术方向

---

### Sprint 43

长截图控制面板与预览窗口 SwiftUI 化

状态：
✅ Done

成果：

- 长截图控制面板与预览窗口完成 SwiftUI 内容迁移
- 长截图模式下的 AI 入口交互得到统一
- 进入长截图模式后预览反馈更明确
- 长截图相关窗口样式与布局规则进一步收口
- 长截图控制面板高度异常已完成修复并人工验证通过

---

### Sprint 42

结果窗口 SwiftUI 化与 AppKit 布局规范

状态：
✅ Done

成果：

- OCR 结果窗口内容层迁移为 SwiftUI
- AI 结果窗口内容层迁移为 SwiftUI
- 保留 AppKit `NSPanel` 外壳与窗口摆放逻辑
- AppKit 外壳布局使用 SnapKit
- AI 结果窗口在窄宽度下补齐按钮自适应换行布局
- 长截图控制面板按钮颜色改为跟随系统外观
- 明确后续 UI 实现优先级：SwiftUI 优先，AppKit 布局优先 SnapKit，最后才使用手动 `frame`

---

## 后续规划

### Sprint 16

标注增强

状态：
✅ Done

目标：

- 增加马赛克
- 补齐最小撤销体验

成果：

- 增加马赛克标注
- 增加最小撤销体验
- 马赛克结果进入复制与保存导出
- 马赛克效果调整为局部模糊遮挡

---

### Sprint 17

截图区域微调

状态：
✅ Done

目标：

- 在截图预览态支持截图区域整体拖动微调
- 鼠标进入可拖动截图区域时显示手型
- 已有任意标注后锁定截图区域，不再允许拖动
- 保持复制、保存与标注导出结果基于拖动后的最终区域

成果：

- 支持截图区域整体拖动微调
- 支持手型与拖动中的鼠标反馈
- 拖动后复制与保存结果与最终区域一致
- 任意标注产生后锁定截图区域

---

### Sprint 18

截图区域缩放微调

状态：
✅ Done

成果：

- 支持拖拽截图区域四边调整尺寸
- 支持拖拽截图区域四角调整尺寸
- 鼠标位于边或角上时显示对应方向双向箭头
- 缩放后复制与保存结果与最终区域一致
- 任意标注产生后锁定截图区域，不再允许移动或缩放

---

### Sprint 19

应用命名调整

状态：
✅ Done

成果：

- 将应用面向用户展示的名称统一从 `TShot` 调整为 `SmartShot`
- 补齐系统权限授权页面中的 `SmartShot` 命名一致性
- 将 `Bundle Identifier` 调整为 `com.sheldon.SmartShot`
- 保持菜单栏、设置页、归档产物与系统权限页中的用户可见名称一致
- 使用 `Archive` 导出的 app 验证通过

---

### Sprint 20

圆角样式滑块

状态：
✅ Done

成果：

- 将顶部圆角开关改为圆角滑块
- 当滑块值为 `0` 时表示无圆角
- 当滑块值增大时，截图预览与最终导出的圆角半径同步增大
- 保持阴影、复制、保存、标注链路不受影响
- 圆角滑块最大值提升到 `100`
- 导出圆角与预览圆角保持一致

---

### Sprint 21

马赛克毛玻璃与交互

状态：
✅ Done

成果：

- 让截图编辑态与最终导出都使用 `CIGaussianBlur` 呈现毛玻璃马赛克
- 支持在编辑态中对马赛克区域进行拖动与缩放调整
- 保持截图拖拽完成后不出现新的明显闪屏
- 修复马赛克保存时由光标递归刷新引发的崩溃
- 修复马赛克内部手型与边角双向箭头未及时显示的问题

---

### Sprint 22

截图开始即冻结屏幕

状态：
✅ Done

成果：

- 热键触发截图后立即冻结当前屏幕画面
- 让暗色遮罩覆盖在冻结画面之上，而不是覆盖在实时桌面之上
- 保持拖拽选区阶段画面不再随桌面内容实时变化
- 让后续预览编辑、复制、保存都基于同一份冻结画面缓存
- 让冻结缓存、预览内容与最终导出结果都不包含鼠标光标
- 在复制或保存完成后清理全屏截图缓存并结束本次截图会话

---

### Sprint 23

截图动作扩展

状态：
✅ Done

成果：

- 在工具栏中增加 OCR 入口
- 在工具栏中增加 Pin 入口
- 统一截图结果的手动触发式工作流
- OCR 改为用户手动触发
- OCR 成功后输出识别结果到 Console
- OCR 成功后将识别文本复制到剪贴板并结束当前编辑态
- OCR 仅识别原始截图区域内容，不携带矩形、圆形、箭头等编辑内容
- Pin 成功后弹出单实例浮动截图窗口
- Pin 成功后结束当前截图会话

---

### Sprint 24

截图反馈与 Pin 窗口增强

状态：
✅ Done

成果：

- 增加 Toast 提示反馈
- 在 OCR、复制、保存结束后弹出对应提示
- 优化 Pin 浮动截图窗口的默认尺寸与默认位置
- 支持通过拖拽窗口边和角调整 Pin 窗口大小

---

### Sprint 25

滚动长截图

状态：
✅ Done

成果：

- 支持滚动区域捕获
- 生成长图结果
- 保持与工具栏编辑态衔接
- 长截图模式下保持截图边界持续可见
- 点击 `长截图` 后切换为实时底层内容滚动捕获
- 长截图结果态支持 `复制长图` 与 `保存长图`
- 长截图取消后直接结束当前截图会话
- 修复长截图导出结果包含白色边框的问题

---

### Sprint 26

长截图增强

状态：
✅ Done

成果：

- 支持滚动过程中自动追加当前屏
- 在截图边框可用侧实时显示当前长图预览
- 长截图操作面板调整为底部 `取消 / 保存 / 复制`
- 保持与现有长截图主链路衔接
- 长截图自动追加从轮询切换为滚轮事件驱动
- 长截图拼接缝增加最小优化，半截文字问题明显改善
- 记录两项后续优化：进一步增强 seam 搜索策略、微调滚轮后的抓帧时机

---

### Sprint 27

设置页 OCR 开关移除

状态：
✅ Done

成果：

- 从设置界面移除 `OCR` 开关
- 保持编辑界面中的 `OCR` 工具栏入口可正常使用
- 移除与设置页 `OCR` 开关相关的无效配置与文档表述

---

### Sprint 28

OCR 结果预览

状态：
✅ Done

成果：

- 点击编辑界面 `OCR` 后，在当前截图编辑态打开 OCR 结果预览窗口
- OCR 结果预览窗口位于选区左右可用空间更大的一侧
- OCR 结果预览窗口提供 `复制` 与 `取消`
- 点击 `复制` 后将 OCR 文本复制到剪贴板并结束当前截图编辑
- 点击 `取消` 后仅关闭 OCR 预览窗口，保留当前截图编辑态
- OCR 空结果显示明确提示并禁用复制
- OCR 失败时保留截图编辑态

---

### Sprint 29

AI 分析

状态：
✅ Done

成果：

- 在工具栏中增加最小 AI 分析入口
- 基于截图或 OCR 结果发起最小 AI 请求
- 输出最小分析结果
- 结果面板支持 `复制`、`重试`、`关闭`
- 缺少配置、OCR 为空、请求失败、空结果时给出明确提示

---

### Sprint 30

AI 结果增强

状态：
✅ Done

成果：

- AI 输出已收敛为 `报错大意`、`可能原因`、`建议下一步`
- AI 结果面板已按区块展示结构化内容
- 支持 `复制全部` 与 `复制建议`
- 发给 AI 前已增加最小 OCR 文本清洗
- 对低质量、不完整或空洞 AI 结果增加了最小兜底反馈
- 修复 AI 结果面板约束冲突与内容顺序反转问题

---

### Sprint 31

窗口悬停选择

状态：
✅ Done

成果：

- 热键触发后支持悬停命中整个应用窗口
- 点击候选窗口后直接进入现有编辑态
- 保持按下拖拽时继续走现有自由框选流程
- 空白区域或无效窗口时回退为普通待命状态
- 窗口悬停确认后的复制、保存、`OCR`、`AI`、`Pin` 主链路支持独立窗口捕获
- 修复窗口重叠场景下目标窗口导出内容不完整的问题

---

### Sprint 32

长截图 OCR / AI

状态：
✅ Done

成果：

- 长截图底部操作面板增加 `OCR` 与 `AI` 入口
- 支持基于当前长图结果直接执行 `OCR` 与 `AI`
- 首帧长图在未继续滚动时也可直接分析
- `OCR` 与 `AI` 基于当前已经拼接出的长图结果执行
- 进入分析后不结束长截图会话，关闭结果窗后可继续滚动追加
- `OCR` 与 `AI` 结果窗根据长截图预览窗所在侧反向摆放
- 长截图模式下同一时刻只保留一个结果窗
- 旧 `AI` 异步结果在切换 `OCR`、继续滚动或结束会话后不会晚到重新弹出
- `AI` 分析窗口在屏幕空间不足时支持底部按钮自动换行

---

### Sprint 33

自定义截图热键

状态：
✅ Done

成果：

- 将 `Settings` 中的截图快捷键配置从预设菜单调整为可点击编辑框
- 支持直接录入 `Command / Shift / Option / Control` 与 `字母 / 数字 / 功能键 / 方向键` 组合
- 用户按 `Return` 后可确认新热键并立即尝试生效
- 用户按 `Esc` 或点击编辑框外部后可取消编辑并恢复旧热键
- 注册失败时会恢复旧热键并给出明确提示
- 兼容旧快捷键存储值并支持新的 `keyCode:modifiers` 存储格式
- 补齐编辑框高亮反馈、点击外部取消与重新打开设置页后状态重置

---

### Sprint 34

AI 视觉取字分析

状态：
✅ Done

成果：

- 在 `Settings` 中增加 `AI 使用视觉取字` 开关，默认关闭
- 开关关闭时保留当前 `本地 OCR -> AI 文本分析` 链路
- 开关打开时切换为 `AI 视觉取字 -> AI 文本分析` 链路
- 普通截图与长截图中的 `AI` 分析入口都已支持双链路切换
- 保持 `OCR` 按钮、`OCR` 结果预览与当前 `AI` 结果窗结构不变
- 长截图 `AI` 链路继续保留原有异步失效保护
- 视觉取字无有效文字时仅提示 `AI 未识别到有效文字`，不再误报为识别失败
- 控制台已补充链路日志，可明确区分当前走的是本地 OCR 还是视觉取字

---

### Sprint 35

AI 菜单与摘要总结

状态：
✅ Done

成果：

- 点击 `AI` 按钮后先弹出最小菜单
- 菜单先支持 `开发报错分析` 与 `摘要总结` 两项
- `摘要总结` 作为本轮唯一新增 AI 能力，输出固定 3 条重点
- 普通截图与长截图中的 `AI` 入口都使用同一套菜单交互
- 继续复用 Sprint 34 的本地 OCR / 视觉取字双输入链路
- 继续复用当前 `AI` 结果窗容器，不新增第二种 AI 结果窗
- 普通截图与长截图中的 `AI` 结果窗都支持按当前方向复制全部或复制重点
- 长截图中的 `AI` / `OCR` 结果窗会优先摆放在长截图预览窗对侧，空间不足时退回同侧

---

### Sprint 36

AI 翻译语言

状态：
✅ Done

成果：

- 在当前 `AI` 菜单中新增 `翻译语言` 子菜单
- 子菜单先支持 `翻译成中文` 与 `翻译成英文`
- 普通截图与长截图中的翻译能力都继续复用 Sprint 34 的双输入链路
- 翻译结果继续复用当前 `AI` 结果窗容器，不新增第二种结果窗
- 本轮结果窗只展示译文，并支持 `复制译文`
- `复制全部` 与 `复制译文` 点击后都只关闭结果窗，不退出当前模式
- 翻译结果已兼容 AI 直接返回纯译文正文的情况，不再误报结果不完整
- 无有效文字时会稳定显示 `AI 未识别到有效文字` 提示，不再被截图层级遮挡

---

### Sprint 37

界面结构识别

状态：
✅ Done

成果：

- 在当前 `AI` 菜单中新增一级项 `界面结构识别`
- 普通截图与长截图都支持相同入口
- 继续复用 Sprint 34 的双输入链路
- 继续复用当前 `AI` 结果窗容器
- 输出单段结构化文本，覆盖设计信息与实现提示
- 本轮支持 `复制全部` 与 `复制结构`
- 结构化文本固定收口为：
  - `组件识别`
  - `结构层级`
  - `视觉特征`
  - `交互语义`
  - `实现提示`
- 无有效内容时已统一提示 `AI 未识别到有效内容`

---

### Sprint 38

App 本地化

状态：
✅ Done

成果：

- 为 App 建立统一的本地化资源基础设施
- 支持简体中文、English、日本語、한국어、Deutsch、Français
- Settings 中支持“跟随系统 + 手动覆盖语言”
- 菜单栏、Settings、截图编辑态、OCR 窗口、AI 窗口文案本地化
- AI prompt、AI 固定输出标题与 AI 输出语言跟随当前有效语言切换
- OCR 识别语言改为“当前语言优先 + 中英兜底”
- 翻译目标语言继续独立于 UI 语言
- 新打开窗口与新触发流程使用新语言

---

### Sprint 39

App 外观切换

状态：
✅ Done

成果：

- 为 App 增加统一的外观模式设置
- Settings 中支持“跟随系统 + 浅色 + 深色”
- 用户切换后当前已打开的 App 自有窗口立即切换
- 新打开窗口与新触发流程继承当前外观
- 外观设置文案纳入现有本地化体系

---

### Sprint 40

工具栏 SF Symbols

状态：
✅ Done

成果：

- 将截图编辑态工具栏升级为以 SF Symbols 为主的混合工具栏
- 6 个标注工具切换为图标按钮
- 撤销 / Pin / 复制 / 保存 / 取消 切换为图标按钮
- `长截图` / `OCR` / `AI` 保持文字按钮
- 工具栏采用单一毛玻璃容器 + 透明按钮风格
- 标注工具选中态改为青色高亮
- 标注工具选中时增加轻量缩放与透明度反馈
- 重复点击同一个标注工具时保持选中状态
- 为截图覆盖层补充自定义 hover 提示气泡

---

### Sprint 41

矩形属性面板

状态：
✅ Done

目标：

- 新增 RGBColor/RectangleProperties 数据模型
- 新增 RectanglePropertyPanelView 属性面板
- 画布选中交互与基于 RectangleProperties 的矩形渲染
- 面板生命周期：点击矩形→显示；点击其他工具/取消→隐藏

成果：

- 矩形工具具备独立属性控制能力
- 支持粗细/透明度/圆角/实心空心/颜色调整
- 点击已画矩形可选中并调整属性
- 面板值作为新矩形默认值

---

### Sprint 47

箭头端点与曲线控制节点

状态：
✅ Done

目标：

- 箭头标注选中后，取消青色虚线边框，改为显示白色圆形控制节点
- 直线箭头选中后显示 2 个端点控制节点，支持端点单独拖拽
- 曲线箭头选中后显示 4 个控制节点（2 个端点 + 2 个曲线控制点）
- 曲线控制节点支持拖拽调节曲线弧度方向和大小
- 箭头主体区域支持按住拖动整体移动（含控制点跟随）

成果：

- 箭头绘制已使用 ArrowProperties 中的存储控制点替代硬编码默认曲线
- `ArrowProperties` 新增 `curveControl1` / `curveControl2` 可选字段
- 新增 `arrowControlPoints`、`arrowInteractionTarget`、`arrowInteraction` 等全套交互方法
- 新增 `AnnotationResizeTarget` 的 `.resizeArrowControl1` / `.resizeArrowControl2` 枚举值
- 新增 `updateSelectedArrow`、`updatedArrowFromResize`、`updateArrowHover` 交互方法
- 箭头导出渲染（CaptureSessionService）同步使用存储控制点
- `hitTestEditableAnnotation` 支持通过控制节点选中箭头
- 属性面板切换 `isCurved` 时自动维护控制点数据一致性
- 选中后移动箭头时，曲线控制点同步跟随移动
- 142 个自动化测试全部通过

---

### Sprint 48

画笔标注选中交互优化

状态：
✅ Done

目标：

- 移除画笔标注选中状态下的青色虚线边框
- 改为在线条首尾两端显示白色圆形控制节点
- 支持拖拽端点控制节点调整线条端点位置
- 保持按住线条主体拖拽整体移动能力

成果：

- 选中画笔标注后已不再显示虚线边框
- 线条起点和终点各显示一个白色圆形控制节点（红色描边）
- 新增 `penInteraction`/`penInteractionTarget`/`updateSelectedPen`/`updatedPenFromResize`/`updatePenHover` 全套交互方法
- 拖拽起点控制节点可调整起点位置，终点保持固定
- 拖拽终点控制节点可调整终点位置，起点保持固定
- 拖拽线条本体可整体移动标注，线条形态不变
- 控制节点悬停时有 `openHand` 光标反馈
- `hitTestEditableAnnotation` 支持通过端点控制节点选中画笔
- 142 个自动化测试全部通过

---

### Sprint 49

马赛克标注选中交互与属性面板联动

状态：
✅ Done

目标：

- 马赛克标注选中后显示青色虚线边框 + 8 个控制节点（4 角 + 4 边中点）
- 修复选中后属性面板不联动的问题（大小滑块、马赛克/毛玻璃模式切换）
- 保持拖拽控制点调整大小和整体移动功能

成果：

- 选中马赛克标注后，虚线边框外增加 8 个白色圆形控制节点（与矩形/椭圆风格一致）
- 控制节点绘制复用 `resizeControlPoints(for:)` 方法
- 修复 `mosaicInteraction` 不检查 `selectedAnnotationIndex` 的问题
- 马赛克选中交互与矩形/椭圆/线条/箭头等工具保持一致（第一击选中→第二击编辑）
- 第一击点击马赛克时正确触发 `onAnnotationSelected`
- 属性面板调整大小/切换马赛克毛玻璃模式通过 `updateSelectedAnnotationProperties(.mosaic(properties))` 实时生效
- 142 个自动化测试全部通过

---
