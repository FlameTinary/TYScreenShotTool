# AGENTS.md

## 项目定位

这是一个面向真实用户的产品项目。

目标：

* 持续交付可用功能
* 支持多人协作开发
* 保持代码长期可维护
* 在真实使用中持续演进

开发方式：

* 人类工程师 + AI 协作开发（Claude Code / Codex）

---

# 核心原则

## 1. 结果导向

优先目标：

* 功能正确
* 可运行
* 可验证

避免：

* 过度设计
* 过度文档
* 为技术而技术

---

## 2. 决策优先级

当规则冲突时：

1. 正确性
2. 可维护性
3. 简单实现
4. 开发效率
5. 架构整洁

---

# 工作模式

根据复杂度自动选择：

## Task

适用：

* 小功能
* Bug 修复
* 局部优化

执行：

实现 → 编译 → 验证

规则：

* 不写设计文档
* 不写计划文档
* 不更新全局文档

---

## Feature（默认）

适用：

* 一个完整功能点
* 涉及多个文件

执行：

需求 → 简要设计（3~5行） → 实现 → 编译 → 验证

---

## Sprint

适用：

* 多模块协作
* 复杂功能
* 重要里程碑

执行：

需求分析 → 系统设计 → 拆解 Feature → 实现 → 测试 → 文档收尾

---

## 任务判断与计划

判断优先依据：

1. 影响范围
2. 是否涉及架构决策
3. 是否涉及多个模块
4. 是否存在不确定实现路径

Task：

* 已知实现方式
* 不需要设计
* 不影响其他模块

Feature：

* 用户可感知
* 涉及多个文件或组件
* 需要轻度设计决策

Sprint：

* 产品级变化
* 架构变化
* 多 Feature 组合

如果无法明确归类：

* 默认按 Feature 执行

需要计划时机：

* 多模块
* 逻辑复杂
* 不确定实现

否则直接实现

---

# AI 协作规则

## AI 默认行为

* 优先实现功能
* 控制改动范围
* 保持代码可运行

---

## AI 修改范围

允许：

* 修改相关文件
* 小范围结构调整

禁止：

* 修改无关模块
* 无理由重构
* 大规模结构变更

---

## AI 输出要求

优先：

* 可运行代码

减少：

* 解释性文字
* 长文档

---

# 技术策略

* 技术选型开放
* 优先已有方案
* 控制复杂度

## UI 技术规则

默认：

* UI 使用 AppKit + SnapKit
* 普通界面布局优先 SnapKit
* 只有窗口定位、Overlay 绘制、逐像素控制等低层场景才使用手动 frame

禁止：

* 新增 SwiftUI 页面或控件
* 新增 NSHostingView / NSHostingController 桥接
* 无明确理由引入第二套 UI 技术栈

---

# 架构策略

原则：

* 简单优先
* 渐进演进

当出现以下情况再优化：

* 重复 ≥ 2 次
* 难以维护
* 修改困难

---

# 文档策略

默认：

* 代码优先，文档跟随
* 除 Task 外，文档在任务收尾前统一更新

文档职责：

* `README.md`：项目目标、功能说明、技术栈
* `docs/ROADMAP.md`：当前方向、已完成、待实现
* `docs/SPRINTS/Sprint-XX.md`：当前目标、范围、实现、验证结果
* `docs/DEVLOG.md`：重要结果、关键问题、决策

执行范围：

* 先看 `docs/ROADMAP.md` 的 `状态` 字段
* 默认以第一个状态不是 `✅ Done` 的 Sprint 作为当前 Sprint
* 对应的 `docs/SPRINTS/Sprint-XX.md` 作为细化范围

更新要求：

Task：

* 不更新文档
* 直接实现

Feature：

* 简要设计
* 收尾前按需更新相关文档

Sprint：

* 按完整流程执行
* 收尾前更新当前 Sprint 文档与 `docs/ROADMAP.md`
* 按需更新 `docs/DEVLOG.md`

文档内容优先关注：

* 做了什么
* 如何验证
* 是否可用

避免：

* 记录过程细节
* 写成流水日志

---

# Git 协作规则

## 分支策略

* main：稳定分支
* feature/*：功能分支

---

## 合并策略

默认：

使用 squash merge

目的：

* 保持主分支历史清晰
* 避免碎片化提交

---

## rebase 使用规则

仅用于：

* 整理 feature 分支提交
* 同步最新 main

禁止：

* 在公共分支使用 rebase

---

## 提交流程

推荐：

1. 确认改动范围
2. git add
3. git commit
4. git push

建议检查：

* 是否包含无关文件
* 是否完成当前任务

---

## Commit 规范

格式：

type(scope): 描述

示例：

feat(capture): 支持截图保存
fix(hotkey): 修复快捷键问题

---

## 提交原则

* 小步提交
* 保证可运行
* 不提交无关内容

---

## 回滚策略

* 出问题优先回滚 feature
* 不在 main 上直接修复杂逻辑

---

## AI 提交要求

* 提交前保证代码可运行
* 控制提交粒度
* 避免一次修改过多文件

---

# 修改范围控制

默认：

只修改当前任务相关代码

禁止：

* 无关修改
* 无目的结构调整

---

# 工作风格

* 稳定推进
* 控制复杂度
* 持续交付

核心：

功能正确 > 完美设计
代码 > 文档
