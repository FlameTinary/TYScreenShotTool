# AGENTS.md

## 项目定位

这是一个面向真实用户的产品项目。

目标：

* 持续交付可用功能
* 保持代码长期可维护
* 支持产品持续演进
* 快速响应用户反馈

开发方式：

* 人类工程师 + Codex 协作开发

---

# 核心目标

持续交付用户可用价值。

判断标准：

* 功能是否可用
* 是否解决用户问题
* 是否能够稳定运行

而不是：

* 文档是否完整
* 设计是否完美
* 架构是否绝对优雅

---

# 核心原则

## 1. 结果导向

优先目标：

* 功能正确
* 可运行
* 可验证

避免：

* 过度设计
* 过度抽象
* 为技术而技术

---

## 2. 决策优先级

当规则冲突时：

1. 正确性
2. 可维护性
3. 简单实现
4. 开发效率
5. 架构整洁

---

## 3. 功能优先

优先级：

```text
功能正确
>
代码整洁
>
架构优雅
```

---

## 4. 简单优先

优先选择：

* 已验证方案
* 项目已有方案
* 官方推荐方案

避免：

* 引入不必要依赖
* 复杂架构
* 提前优化

---

# 开发前检查

实现前必须优先检查：

1. 是否已有类似功能
2. 是否已有可复用组件
3. 是否已有相同技术方案

原则：

```text
先复用
↓
再扩展
↓
最后新增
```

禁止：

* 未检查现有实现直接新增模块
* 未确认必要性直接引入新依赖

---

# 工作模式

默认按 Feature 执行。

---

## Task

适用：

* Bug 修复
* 小功能
* 局部优化

特点：

* 已知实现方式
* 不涉及架构决策
* 不影响其他模块

执行：

```text
实现
↓
编译
↓
测试（如存在）
↓
验证
```

规则：

* 不写设计
* 不写计划
* 不更新文档

---

## Feature（默认）

适用：

* 完整功能点
* 用户可感知变化
* 涉及多个文件

执行：

```text
需求
↓
简要设计（3~5行）
↓
实现
↓
编译
↓
测试
↓
验证
↓
收尾
```

设计内容仅包含：

* 实现思路
* 涉及模块
* 验证方式

避免长篇设计文档。

---

## Sprint

适用：

* 产品级功能
* 多模块协作
* 架构调整
* 重要里程碑

执行：

```text
需求分析
↓
系统设计
↓
拆分 Feature
↓
实现
↓
测试
↓
收尾
```

---

# 任务判断规则

判断依据：

1. 影响范围
2. 是否涉及架构决策
3. 是否涉及多个模块
4. 是否存在实现不确定性

默认：

```text
Feature
```

只有明确属于 Task 或 Sprint 时才切换。

---

# Feature 控制原则

单个 Feature 应满足：

* 可独立开发
* 可独立验证
* 可独立合并

避免：

一个 Feature 包含多个独立目标。

优先：

用户可见功能切片

而不是：

技术层切片

---

# AI 行为规范

## 默认行为

优先：

* 实现功能
* 保持可运行
* 控制改动范围
* 完成验证

---

## 默认直接实现

对于明确需求：

默认直接实现。

仅当出现以下情况时先设计：

* 存在多种实现方案
* 涉及架构决策
* 风险较高
* 需求不明确

---

## 修改范围控制

允许：

* 修改当前任务相关代码
* 小范围结构调整

禁止：

* 修改无关模块
* 大规模结构调整
* 无理由修改历史代码

---

## 禁止过度重构

禁止：

* 为了更优雅而重构
* 为了统一风格而重构
* 为了理论最佳实践而重构

除非：

当前任务必须修改相关结构。

---

## 删除代码规则

允许删除：

* 明确未使用代码
* 已废弃实现
* 当前任务替换的旧实现

禁止删除：

* 无法确认用途代码
* 未验证影响范围代码

原则：

保守修改。

---

## 输出要求

优先输出：

* 可运行代码
* 修改说明
* 验证结果

减少：

* 长篇解释
* 理论分析
* 冗余说明

---

# 文档策略

默认：

```text
代码优先
文档跟随
```

---

## 文档职责

### PROJECT_CONTEXT.md

维护：

* 项目当前阶段
* 当前有效产品能力
* 技术与架构基线
* 当前协作入口与文档地图

说明：

* 这是必要文档
* 进入开发前优先阅读
* 当项目阶段、主技术方向、主流程或协作入口发生变化时必须同步更新

---

### README.md

维护：

* 项目介绍
* 功能说明
* 技术栈
* 使用方式

---

### docs/ROADMAP.md

维护：

* 已完成内容
* 当前方向
* 后续规划

---

### docs/SPRINTS/

维护：

* 当前 Sprint 目标
* 范围
* 验证结果

---

# 当前工作范围

默认参考：

* PROJECT_CONTEXT.md
* docs/ROADMAP.md
* 当前 Sprint

执行顺序：

1. PROJECT_CONTEXT.md
2. ROADMAP
3. 当前 Sprint

如果用户明确指定任务：

用户任务优先级最高。

---

## 文档更新规则

### Task

不更新文档。

---

### Feature

收尾前按需更新：

* PROJECT_CONTEXT.md
* README.md
* ROADMAP.md

---

### Sprint

收尾前更新：

* PROJECT_CONTEXT.md
* 当前 Sprint 文档
* ROADMAP.md

必要时更新：

* README.md

---

## 文档创建规则

禁止主动创建：

* PLAN.md
* DESIGN.md
* IMPLEMENTATION_PLAN.md
* REVIEW.md
* REPORT.md
* TODO.md

除非用户明确要求。

优先更新已有文档。

---

# Git 协作规则

详细规则见：

```text
GIT_WORKFLOW.md
```

执行开发任务前：

必须阅读并遵守 GIT_WORKFLOW.md。

---

# 构建与测试

## 验证顺序

```text
1. 编译通过
2. 自动测试通过（如存在）
3. 功能符合需求
4. 无明显回归
```

优先验证：

用户可见行为。

---

## 脚本优先原则

执行构建、归档和测试时：

优先使用项目脚本。

### 构建

```bash
./scripts/build.sh
```

---

### 测试

```bash
./scripts/test.sh
```

---

### 归档

```bash
./scripts/archive.sh
```

---

仅在排障时直接使用：

```bash
xcodebuild
```

---

# 工作风格

稳定推进

控制复杂度

持续交付

原则：

```text
完成 > 完美

交付 > 设计

代码 > 文档
```

核心：

```text
功能正确 > 完美设计
```
